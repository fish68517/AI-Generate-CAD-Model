# CADmium 本机轻量复现运行指南

本指南对应当前机器：Windows 11、32GB 内存、GTX 1050 Ti 4GB。目标是完成数据抽样、数据审计、CPU 几何重建、静态/交互式可视化和低资源推理探测，不执行论文规模训练。

## 1. 进入项目并激活环境

在 PowerShell 中执行：

```powershell
Set-Location 'E:\code\论文复现'
.\.venv\Scripts\Activate.ps1
```

如果要重新建立环境：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements-local.txt
```

## 2. 环境自检

```powershell
.\.venv\Scripts\python.exe .\scripts\check_environment.py
```

结果保存到 `reports/environment.json`。重点看：

- `tools.nvidia_smi`：系统是否识别显卡；
- `torch.cuda_available`：当前 PyTorch 能否真正调用 CUDA；
- `packages`：轻量复现依赖版本。

系统能识别显卡不代表 PyTorch 能使用显卡。必须以 `torch.cuda_available=true` 为准。

## 3. 下载平衡抽样数据

```powershell
.\.venv\Scripts\python.exe .\scripts\download_sample.py
```

脚本从公开 test split 流式扫描，固定抽取 20 条，分为：Cut、多 part、圆弧、圆、纯直线五类，每类 4 条。主要输出：

```text
data/samples/test_sample.jsonl
data/samples/sample_manifest.json
data/samples/json/*.json
data/samples/prompts/*.txt
```

## 4. 数据审计

```powershell
.\.venv\Scripts\python.exe .\scripts\audit_dataset.py
```

输出：

```text
reports/data_audit/data_audit.json
reports/data_audit/data_audit.png
```

默认使用 Qwen2.5-Coder tokenizer，并以 768 token 作为本机低长度试验线，统计被截断比例。官方系统 schema 及一条简单提示约为 688 token，因此推理不应降到 512，否则用户提示可能被截断。

## 5. CPU 几何重建和可视化

```powershell
.\.venv\Scripts\python.exe .\scripts\reconstruct_json.py
```

每条样例输出：

```text
reports/reconstruction/<uid>/input.json
reports/reconstruction/<uid>/model.stl
reports/reconstruction/<uid>/preview.png
reports/reconstruction/<uid>/interactive.html
reports/reconstruction/<uid>/status.json
```

双击 `interactive.html` 可以旋转和缩放模型。汇总结果位于：

```text
reports/reconstruction/summary.json
reports/reconstruction/reconstruction_summary.png
```

该轻量重建器使用 Shapely + Trimesh/Manifold，支持 CADmium 的 line、arc、circle、双向拉伸和四类布尔操作。它用于本机闭环与展示，不能冒充论文 PythonOCC 评价结果。

## 6. 低资源推理

先运行不加载大权重的验证：

```powershell
.\.venv\Scripts\python.exe .\scripts\infer_low_resource.py --mode dry-run
```

它会检查 1.5B adapter 元数据、对应基座、tokenizer、系统提示和 768-token 截断链路。

只有当 `reports/environment.json` 中 `torch.cuda.cuda_available=true` 时，才尝试 4bit GPU：

```powershell
.\.venv\Scripts\python.exe .\scripts\infer_low_resource.py --mode cuda-4bit --max-input-tokens 768 --max-new-tokens 128
```

CPU 内存充足时可以尝试短生成：

```powershell
.\.venv\Scripts\python.exe .\scripts\infer_low_resource.py --mode cpu-fp32 --max-input-tokens 768 --max-new-tokens 64
```

`cpu-int8` 会在加载基座和合并 LoRA 后执行动态量化，转换阶段可能短时需要较多内存：

```powershell
.\.venv\Scripts\python.exe .\scripts\infer_low_resource.py --mode cpu-int8 --max-input-tokens 768 --max-new-tokens 64
```

无论成功或失败，详细原因都会保存到 `reports/inference/attempt_<mode>.json`，不会使用 `eval()` 执行模型文本。

## 7. 单元测试

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

测试至少覆盖矩形拉伸体积和带圆孔实体拓扑。
